# HTML directory
# Where the HTML, CSS, JavaScript, and assetes files for the fronten stored
# Leave it default value because the installer use this location
HTML_DIR=/var/www/streamtfhd/html

# Port
PORT=8080